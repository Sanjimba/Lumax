const panel=document.getElementById("panel"),login=document.getElementById("login"),app=document.getElementById("app"),authMessage=document.getElementById("authMessage");let current="trust_steps",items=[];
const labels={trust_steps:"Confiança / Passos",services:"Serviços",properties:"Imóveis",posts:"Publicações",gallery:"Galeria",faq:"FAQ"};const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&gt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
async function request(url,options={}){let response;try{response=await fetch(`/api/${url}`,options)}catch{throw new Error("Não foi possível contactar o servidor. Verifique a ligação e tente novamente.")}const type=response.headers.get("content-type")||"";let body=null;if(response.status!==204&&type.includes("application/json")){try{body=await response.json()}catch{throw new Error("O servidor respondeu de forma inesperada. Tente novamente daqui a instantes.")}}else if(response.status!==204&&!response.ok){throw new Error("O servidor respondeu de forma inesperada. Tente novamente daqui a instantes.")}if(response.status===401){login.hidden=false;app.hidden=true;throw new Error(body?.error||"A sessão terminou. Inicie sessão novamente.")}if(!response.ok)throw new Error(body?.error||"Não foi possível concluir o pedido.");return body}
function authAlert(message,type="error"){authMessage.textContent=message;authMessage.className=`alert ${type}`;authMessage.hidden=!message}function setLoading(button,loading){button.disabled=loading;button.querySelector(".button-text").hidden=loading;button.querySelector(".spinner").hidden=!loading}function showAuthForm(name){["loginForm","setupForm","recoveryForm","resetForm"].forEach(id=>document.getElementById(id).hidden=id!==name);authAlert("")}async function boot(){try{await request("auth/session");login.hidden=true;app.hidden=false;show(current);return}catch{}try{const status=await request("auth/setup-status");if(status.setupRequired){showAuthForm("setupForm");document.getElementById("authTitle").textContent="Crie a conta de administrador";document.getElementById("authIntro").textContent="É o primeiro acesso à área reservada."}else showAuthForm("loginForm")}catch{authAlert("Não foi possível verificar o acesso. Tente atualizar a página.")}}document.getElementById("loginForm").onsubmit=async e=>{e.preventDefault();const b=document.getElementById("loginButton");authAlert("");setLoading(b,true);try{await request("auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:user.value,password:pass.value})});login.hidden=true;app.hidden=false;show(current)}catch(error){authAlert(error.message)}finally{setLoading(b,false)}};document.getElementById("setupForm").onsubmit=async e=>{e.preventDefault();const b=document.getElementById("setupButton");if(setupPass.value!==setupPassConfirm.value)return authAlert("As palavras-passe não coincidem.");setLoading(b,true);try{const result=await request("auth/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:setupUser.value,password:setupPass.value})});showAuthForm("loginForm");user.value=setupUser.value;pass.focus();document.getElementById("authTitle").textContent="Conta criada";authAlert(result.message,"success")}catch(error){authAlert(error.message)}finally{setLoading(b,false)}};document.getElementById("showRecovery").onclick=()=>showAuthForm("recoveryForm");document.querySelectorAll(".auth-back").forEach(b=>b.onclick=()=>showAuthForm("loginForm"));document.getElementById("recoveryForm").onsubmit=async e=>{e.preventDefault();const b=document.getElementById("recoveryButton");setLoading(b,true);try{const result=await request("auth/recovery/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:recoveryUser.value})});authAlert(result.message,"success");document.getElementById("showReset").hidden=false;resetUser.value=recoveryUser.value}catch(error){authAlert(error.message)}finally{setLoading(b,false)}};document.getElementById("showReset").onclick=()=>showAuthForm("resetForm");document.getElementById("resetForm").onsubmit=async e=>{e.preventDefault();const b=document.getElementById("resetButton");if(resetPass.value!==resetPassConfirm.value)return authAlert("As palavras-passe não coincidem.");setLoading(b,true);try{const result=await request("auth/recovery/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:resetUser.value,code:resetCode.value,password:resetPass.value})});showAuthForm("loginForm");authAlert(result.message,"success")}catch(error){authAlert(error.message)}finally{setLoading(b,false)}};
document.getElementById("logout").onclick=async()=>{try{await request("auth/logout",{method:"POST"})}finally{location.reload()}};document.querySelectorAll("[data-tab]").forEach(button=>button.onclick=()=>show(button.dataset.tab));
function fields(tab,x={}){if(tab==="trust_steps")return `<label>Ordem<input name="order" type="number" min="1" value="${esc(x.order||items.length+1)}" required></label><label>Título<input name="title" value="${esc(x.title)}" required></label><label class="full">Descrição<textarea name="description" required>${esc(x.description)}</textarea></label>`;if(tab==="services")return `<label>Título<input name="title" value="${esc(x.title)}" required></label><label class="full">Descrição<textarea name="desc" required>${esc(x.desc)}</textarea></label>`;if(tab==="properties")return `<label>Título<input name="title" value="${esc(x.title)}" required></label><label>Tipo<input name="type" value="${esc(x.type)}"></label><label>Localização<input name="location" value="${esc(x.location)}"></label><label>Preço<input name="price" value="${esc(x.price)}"></label><label class="full">Descrição<textarea name="description">${esc(x.description)}</textarea></label><label class="full">Foto (máximo 5 MB)<input type="file" name="image" accept="image/*"></label>${x.image?`<img class="preview" src="${esc(x.image)}" alt="">`:""}`;if(tab==="posts")return `<label>Categoria<input name="category" value="${esc(x.category)}"></label><label>Título<input name="title" value="${esc(x.title)}" required></label><label class="full">Texto<textarea name="text">${esc(x.text)}</textarea></label>`;if(tab==="gallery")return `<label>Título (opcional)<input name="title" value="${esc(x.title)}"></label><label>Ordem<input name="order" type="number" min="1" value="${esc(x.order||items.length+1)}"></label><label class="full">Imagem<input type="file" name="image" accept="image/*" ${x.id?"":"required"}></label>${x.image?`<img class="preview" src="${esc(x.image)}" alt="">`:""}`;return `<label>Ordem<input name="order" type="number" min="1" value="${esc(x.order||items.length+1)}" required></label><label>Pergunta<input name="question" value="${esc(x.question)}" required></label><label class="full">Resposta<textarea name="answer" required>${esc(x.answer)}</textarea></label>`}
function item(tab,x,index){const title=tab==="faq"?x.question:tab==="trust_steps"?`${x.order}. ${x.title}`:tab==="properties"?`${x.title} — ${x.location||""}`:tab==="gallery"?x.title||`Imagem ${index+1}`:x.title;return `<article class="item"><div><strong>${esc(title)}</strong>${x.image?`<img class="preview" src="${esc(x.image)}" alt="">`:""}</div><div class="actions"><button class="move" data-id="${x.id}" data-way="-1" ${index===0?"disabled":""}>↑</button><button class="move" data-id="${x.id}" data-way="1" ${index===items.length-1?"disabled":""}>↓</button><button class="edit edit-item" data-id="${x.id}">Editar</button><button class="danger del" data-id="${x.id}">Eliminar</button></div></article>`}
async function show(tab){current=tab;if(tab==="settings")return settings();if(tab==="password")return passwordSettings();items=await request(tab);panel.innerHTML=`<div class="toolbar"><div><h1>${labels[tab]}</h1><p class="notice">Os dados são guardados no servidor e publicados no site.</p></div><button id="add">+ Adicionar</button></div><div id="list">${items.map((x,i)=>item(tab,x,i)).join("")||"<p>Ainda não existem registos.</p>"}</div>`;add.onclick=()=>editor(tab);document.querySelectorAll(".edit-item").forEach(b=>b.onclick=()=>editor(tab,items.find(x=>x.id===b.dataset.id)));document.querySelectorAll(".del").forEach(b=>b.onclick=async()=>{if(confirm("Eliminar este registo?")){await request(`${tab}/${b.dataset.id}`,{method:"DELETE"});show(tab)}});document.querySelectorAll(".move").forEach(b=>b.onclick=async()=>{const i=items.findIndex(x=>x.id===b.dataset.id),j=i+Number(b.dataset.way);[items[i],items[j]]=[items[j],items[i]];await request(`${tab}/reorder`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({ids:items.map(x=>x.id)})});show(tab)})}
function editor(tab,x=null){panel.innerHTML=`<h1>${x?"Editar":"Adicionar"} ${labels[tab]}</h1><form class="form" id="editForm"><div class="form-grid">${fields(tab,x||{})}</div><p><button>Guardar</button> <button type="button" id="cancel">Cancelar</button></p><p class="error" id="formError"></p></form>`;cancel.onclick=()=>show(tab);editForm.onsubmit=async e=>{e.preventDefault();try{const fd=new FormData(e.target),options={method:x?"PUT":"POST",body:fd};await request(x?`${tab}/${x.id}`:tab,options);show(tab)}catch(error){formError.textContent=error.message}}}
async function importLegacyContent(){if(!confirm("Importar os dados antigos deste navegador? Os registos serão adicionados aos dados atuais."))return;let imported=0;try{const settings=JSON.parse(localStorage.getItem("lumax_settings")||"null");if(settings&&typeof settings==="object"){await request("settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(settings)});imported++}}catch{}for(const tab of ["trust_steps","services","properties","posts","gallery","faq"]){let legacy;try{legacy=JSON.parse(localStorage.getItem(`lumax_${tab}`)||"[]")}catch{legacy=[]}if(!Array.isArray(legacy))continue;for(const item of legacy){const fd=new FormData();for(const [key,value] of Object.entries(item)){if(key!=="image"&&value!=null)fd.append(key,value)}if(item.image&&String(item.image).startsWith("data:image/")){const blob=await fetch(item.image).then(response=>response.blob());fd.append("image",blob,"imagem-antiga")}else if(item.image)fd.append("image",item.image);await request(tab,{method:"POST",body:fd});imported++}}formError.textContent=imported?`${imported} registo(s) antigo(s) importado(s).`:"Não foram encontrados dados antigos neste navegador.";}
async function settings(){const s=await request("settings"), fields=[['phone','Telefone'],['whatsapp','WhatsApp'],['email','Email'],['address','Morada / área de atuação'],['facebook','Facebook'],['trust_eyebrow','Eyebrow — Confiança'],['trust_title','Título — Confiança'],['trust_subtitle','Subtítulo — Confiança'],['services_eyebrow','Eyebrow — Serviços'],['services_title','Título — Serviços'],['properties_eyebrow','Eyebrow — Imóveis'],['properties_title','Título — Imóveis'],['properties_notice','Aviso — Imóveis'],['faq_eyebrow','Eyebrow — FAQ'],['faq_title','Título — FAQ']];panel.innerHTML=`<h1>Configurações</h1><form class="form" id="setForm">${fields.map(([key,label])=>`<label>${label}<textarea name="${key}">${esc(s[key])}</textarea></label>`).join("")}<button>Guardar configurações</button><button type="button" id="legacyImport">Importar dados do navegador antigo</button><p class="notice">Use a importação apenas no navegador onde os dados antigos estavam guardados em localStorage.</p><p class="error" id="formError"></p></form>`;setForm.onsubmit=async e=>{e.preventDefault();try{await request("settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});formError.textContent="Configurações guardadas."}catch(error){formError.textContent=error.message}};legacyImport.onclick=()=>importLegacyContent().catch(error=>{formError.textContent=error.message})}
boot();

function passwordSettings(){panel.innerHTML=`<h1>Alterar palavra-passe</h1><form class="form" id="passwordForm"><label>Palavra-passe atual<input name="currentPassword" type="password" required autocomplete="current-password"></label><label>Nova palavra-passe<input name="newPassword" type="password" required minlength="12" autocomplete="new-password"></label><label>Confirmar nova palavra-passe<input name="confirmPassword" type="password" required minlength="12" autocomplete="new-password"></label><button>Guardar nova palavra-passe</button><p class="notice">A nova palavra-passe deve ter pelo menos 12 caracteres.</p><p class="error" id="formError"></p></form>`;passwordForm.onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));if(data.newPassword!==data.confirmPassword){formError.textContent="As palavras-passe não coincidem.";return}try{const result=await request("auth/password",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});formError.className="success-message";formError.textContent=result.message;e.target.reset()}catch(error){formError.className="error";formError.textContent=error.message}}}
